id: full-dev
name: 完整开发流程
description: 从构想到完成的开发进度协调流程；不用于上线或发布审批
profile: coordination
steps:
  - id: brainstorm
    skill: brainstorming
    next: [plan]
    skip_if: scope_is_trivial
    gate:
      must_produce: brainstorm_output

  - id: plan
    skill: writing-plans
    next: [develop]
    gate:
      must_produce: implementation_plan
      min_length: 500

  - id: develop
    skill: tdd
    next: [review]
    gate:
      must_produce: code_changes

  - id: review
    skill: pr-review
    next: [finish]
    gate:
      must_produce: review_feedback

  - id: finish
    skill: finish-branch
    next: []
    gate:
      must_produce: merge_confirmation
