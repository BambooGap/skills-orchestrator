---
id: git-worktrees
name: Git Worktrees 工作流
summary: "git worktree 使用方法，同时在多个分支工作，无需 stash 或切换分支"
tags: [git, workflow, parallel]
load_policy: free
---

# Git Worktrees 工作流

git worktree 使用方法，同时在多个分支工作，无需 stash 或切换分支

> 如需完整内容，请使用 `skills-orchestrator sync --full` 导出