# SO022: duplicate-skill-id-blocking

`SO022` is a structural error emitted when the parser sees the same Skill ID more than once in
discovered or explicit configuration. Runtime registry construction stops rather than selecting one
definition by ordering.

This rule is additive. `SO002` remains a stable warning identifier for historical report consumers.
