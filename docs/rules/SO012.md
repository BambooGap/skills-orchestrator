# SO012: required-skill-without-approver

Severity: warning

`builtin/team-standard` expects `load_policy: require` skills to declare `approvers`.

## Why It Matters

Required skills are injected into runtime context automatically, so they need stronger review
evidence than optional skills.

## Fix

```yaml
approvers: [security-review, staff-engineering]
```
