# SO003: unresolved-conflict

Severity: error

A declared skill conflict cannot be resolved by policy and priority.

## Why It Matters

When two effective required skills conflict and have the same priority, the resolver cannot decide
which instruction should win.

## Fix

Choose one:

- raise one skill's `priority`
- lower one skill's `load_policy`
- remove an incorrect `conflict_with` declaration
