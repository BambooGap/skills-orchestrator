# SO001: missing-description

Severity: warning

A skill should include either `summary` or the official Agent Skills `description` field.

## Why It Matters

Routing, search, and generated summaries rely on concise metadata. Missing descriptions make
skill selection less explainable.

## Fix

Add a short metadata field:

```yaml
summary: "Review Python code for correctness, security, and maintainability"
```
