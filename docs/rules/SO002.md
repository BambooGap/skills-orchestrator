# SO002: duplicate-skill-id

Severity: warning

More than one skill resolves to the same skill id.

## Why It Matters

The current parser keeps the first occurrence and skips later duplicates. That behavior remains
compatible, but it can hide accidental copies or stale files.

## Fix

Rename one skill id or remove the duplicate file.
