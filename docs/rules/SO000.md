# SO000: fatal-error

The check could not complete because configuration loading or input parsing failed.

This rule is emitted only for machine-readable output formats so CI systems still receive valid JSON or SARIF when the underlying configuration is broken.

## Fix

Fix the reported configuration path, YAML syntax, missing skill directory, or invalid input, then run `skills-orchestrator check` again.
