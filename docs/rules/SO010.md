# SO010: missing-version

Severity: warning

`builtin/team-standard` expects each shared skill to declare a `version`.

## Why It Matters

Version metadata lets teams review instruction changes like other source-controlled assets.

## Fix

```yaml
version: 1.4.0
```
