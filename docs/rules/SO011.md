# SO011: invalid-lifecycle

Severity: error

`builtin/team-standard` only allows supported lifecycle states.

## Why It Matters

Unrecognized lifecycle values make rollout, deprecation, and retirement policy ambiguous.

## Fix

Use one of:

```yaml
lifecycle: active
```

Allowed values are `active`, `beta`, `deprecated`, and `retired`.
