# SO007: lock-drift

Severity: warning

The current resolved skill state differs from `skills.lock.json`.

## Why It Matters

Lock drift can change which instructions are active, blocked, or inherited without an explicit
review.

## Fix

Review the drift, then regenerate the lock file if the change is intended.
